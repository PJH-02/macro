# Provider Contract Examples

These fixtures are Phase 1 contract examples for the final MVP provider families.
They are intentionally small, human-readable samples used to verify that the repo
keeps at least one request/response example per provider family before adapter
implementation expands in Phase 3.

They are examples, not frozen payload schemas.
